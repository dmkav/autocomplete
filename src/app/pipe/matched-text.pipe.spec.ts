import { MatchedTextPipe } from './matched-text.pipe';

describe('MatchedTextPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchedTextPipe();
    expect(pipe).toBeTruthy();
  });
});
